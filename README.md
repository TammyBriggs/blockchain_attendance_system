# blockchain_attendance_system
A blockchain-based attendance tracking system implemented in C, using SHA-256 cryptographic hashing, ECDSA digital signatures, and file-based student registry validation to ensure immutable and tamper-proof attendance records.
